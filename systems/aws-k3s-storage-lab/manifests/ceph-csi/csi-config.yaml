# 이 파일은 04_csi_install.sh 실행 시 자동 생성됩니다.
# 수동 생성 예시:
apiVersion: v1
kind: ConfigMap
metadata:
  name: ceph-csi-config
  namespace: ceph-csi
data:
  config.json: |
    [
      {
        "clusterID": "<CEPH_FSID>",
        "monitors": ["<BACKEND_PRIVATE_IP>:6789"]
      }
    ]
