apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# BeeGFS CSI Driver upstream overlay를 그대로 사용하되
# csi-beegfs-config.yaml만 오버라이드
# 04_csi_install.sh에서 /tmp/beegfs-csi-driver/deploy/k8s/overlays/default/ 에 적용됨

resources:
  - storageclass-beegfs.yaml
